/**chop a 
  * Task description: Write a method that to string into chunks of a given length
  * Expected Result: (javascript) =>  ["ja", "va", "sc", "ri", "pt"]
  * Task Complexity: 1 of 5
*/
const chop = function (str, chunk) {
  let output = [];
  chop(str).size()
  return output;
}

module.exports = chop;

